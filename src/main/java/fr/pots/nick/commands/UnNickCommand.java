package fr.pots.nick.commands;

import org.bukkit.command.CommandSender;
import org.bukkit.entity.Player;

/**
 * @author MrCubee
 * @since 1.0
 * @version 1.0
 */
public class UnNickCommand extends NickBaseCommand {

    @Override
    public boolean onCommand(CommandSender sender, Player targetPlayer) {
        /*
            TODO: Create /unnick command.
         */
        return true;
    }
}
